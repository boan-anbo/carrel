#[derive(Clone)]
/// Information collected in the processing process that might be useful for other steps.
pub struct Metadata {

}

// impl default Metadata
impl Default for Metadata {
    fn default() -> Self {
        Self {
        }
    }
}

